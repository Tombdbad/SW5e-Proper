// Add to client/src/components/Map/GalacticMap3D/GalaxyBackground.tsx
// or create a new component file

import React, { useMemo, useRef } from 'react';
import { useFrame } from '@react-three/fiber';
import * as THREE from 'three';

interface GalaxyBackgroundProps {
  size?: number;
  density?: number;
  color?: string;
  rotation?: boolean;
}

export const GalaxyBackground: React.FC<GalaxyBackgroundProps> = ({
  size = 1000,
  density = 5000,
  color = '#4060ff',
  rotation = true
}) => {
  const particlesRef = useRef<THREE.Points>(null);
  const spiralDustRef = useRef<THREE.Points>(null);

  // Create background stars
  const starsGeometry = useMemo(() => {
    const geometry = new THREE.BufferGeometry();
    const positions = new Float32Array(density * 3);
    const colors = new Float32Array(density * 3);
    const sizes = new Float32Array(density);

    // Star color variations
    const starTypes = [
      { color: new THREE.Color('#ffffff'), probability: 0.5 }, // White
      { color: new THREE.Color('#b0c0ff'), probability: 0.3 }, // Blue-white
      { color: new THREE.Color('#ffcc99'), probability: 0.15 }, // Yellow
      { color: new THREE.Color('#ff9966'), probability: 0.05 }  // Red
    ];

    for (let i = 0; i < density; i++) {
      // Position stars in a sphere around the scene
      const radius = size * (0.5 + Math.random() * 0.5); // Between 50% and 100% of size
      const theta = Math.random() * Math.PI * 2; // Horizontal angle
      const phi = Math.acos(2 * Math.random() - 1); // Vertical angle
      
      positions[i * 3] = radius * Math.sin(phi) * Math.cos(theta);
      positions[i * 3 + 1] = radius * Math.sin(phi) * Math.sin(theta);
      positions[i * 3 + 2] = radius * Math.cos(phi);
      
      // Choose star color based on probabilities
      let starColor;
      const rand = Math.random();
      let probSum = 0;
      
      for (const type of starTypes) {
        probSum += type.probability;
        if (rand <= probSum) {
          starColor = type.color;
          break;
        }
      }
      
      starColor = starColor || starTypes[0].color; // Default to white if something goes wrong
      
      colors[i * 3] = starColor.r;
      colors[i * 3 + 1] = starColor.g;
      colors[i * 3 + 2] = starColor.b;
      
      // Random star sizes with bias toward smaller stars
      sizes[i] = 0.5 + Math.pow(Math.random(), 3) * 2.5; // Between 0.5 and 3.0
    }
    
    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
    geometry.setAttribute('color', new THREE.BufferAttribute(colors, 3));
    geometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1));
    
    return geometry;
  }, [size, density]);
  
  // Create galaxy dust in spiral arms
  const spiralDustGeometry = useMemo(() => {
    const geometry = new THREE.BufferGeometry();
    const spiralCount = 5;  // Number of spiral arms
    const particlesPerSpiral = 2000;
    const totalParticles = spiralCount * particlesPerSpiral;
    
    const positions = new Float32Array(totalParticles * 3);
    const colors = new Float32Array(totalParticles * 3);
    const sizes = new Float32Array(totalParticles);
    
    // Galaxy dust color
    const dustColor = new THREE.Color(color);
    const dustColorVariation1 = new THREE.Color(color).offsetHSL(0.1, 0, 0);
    const dustColorVariation2 = new THREE.Color(color).offsetHSL(-0.1, 0, 0);
    
    const galaxyRadius = size * 0.4; // Galaxy radius is smaller than background stars
    const spiralTightness = 4; // How tightly wound the spirals are
    
    for (let s = 0; s < spiralCount; s++) {
      const spiralOffset = (s / spiralCount) * Math.PI * 2;
      
      for (let i = 0; i < particlesPerSpiral; i++) {
        const idx = s * particlesPerSpiral + i;
        const posIdx = idx * 3;
        const colorIdx = idx * 3;
        
        // Particle distance from center (more particles toward the edges)
        const distanceRatio = 0.1 + 0.9 * Math.pow(i / particlesPerSpiral, 0.7);
        const distance = galaxyRadius * distanceRatio;
        
        // Spiral angle
        const angle = spiralOffset + distanceRatio * spiralTightness * Math.PI * 2;
        
        // Add some randomness to angle and distance
        const randomAngle = angle + (Math.random() - 0.5) * 0.3;
        const randomDistance = distance * (0.9 + Math.random() * 0.2);
        
        // Calculate position
        positions[posIdx] = Math.cos(randomAngle) * randomDistance;
        positions[posIdx + 1] = (Math.random() - 0.5) * galaxyRadius * 0.1; // Small vertical variance
        positions[posIdx + 2] = Math.sin(randomAngle) * randomDistance;
        
        // Color variation based on distance from center and random factor
        let particleColor;
        const colorRand = Math.random();
        
        if (colorRand < 0.5) {
          particleColor = dustColor;
        } else if (colorRand < 0.75) {
          particleColor = dustColorVariation1;
        } else {
          particleColor = dustColorVariation2;
        }
        
        // Fade color based on distance from center
        const fade = 0.5 + 0.5 * distanceRatio;
        colors[colorIdx] = particleColor.r * fade;
        colors[colorIdx + 1] = particleColor.g * fade;
        colors[colorIdx + 2] = particleColor.b * fade;
        
        // Size variation - dust particles are larger toward center
        sizes[idx] = 2 + (1 - distanceRatio) * 3;
      }
    }
    
    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
    geometry.setAttribute('color', new THREE.BufferAttribute(colors, 3));
    geometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1));
    
    return geometry;
  }, [size, color]);
  
  // Star shader material
  const starsMaterial = useMemo(() => {
    return new THREE.ShaderMaterial({
      uniforms: {
        time: { value: 0 }
      },
      vertexShader: `
        attribute float size;
        attribute vec3 color;
        varying vec3 vColor;
        uniform float time;
        
        void main() {
          vColor = color;
          
          // Subtle star twinkling effect
          float twinkle = sin(time * 0.5 + gl_VertexID * 0.1) * 0.5 + 0.5;
          
          vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
          gl_PointSize = size * (300.0 / -mvPosition.z) * (0.8 + 0.2 * twinkle);
          gl_Position = projectionMatrix * mvPosition;
        }
      `,
      fragmentShader: `
        varying vec3 vColor;
        
        void main() {
          // Create circular points
          float distanceFromCenter = length(gl_PointCoord - vec2(0.5));
          if (distanceFromCenter > 0.5) {
            discard;
          }
          
          // Soft edge falloff
          float strength = 1.0 - (distanceFromCenter * 2.0);
          strength = pow(strength, 1.5);
          
          gl_FragColor = vec4(vColor, strength);
        }
      `,
      transparent: true,
      blending: THREE.AdditiveBlending,
      depthWrite: false
    });
  }, []);
  
  // Dust shader material
  const dustMaterial = useMemo(() => {
    return new THREE.ShaderMaterial({
    uniforms: {
        time: { value: 0 }
      },
      vertexShader: `
        attribute float size;
        attribute vec3 color;
        varying vec3 vColor;
        uniform float time;
        
        void main() {
          vColor = color;
          
          // Add subtle movement to dust
          vec3 pos = position;
          float movement = sin(time * 0.05 + position.x * 0.01 + position.z * 0.01) * 0.5;
          pos.y += movement;
          
          vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);
          gl_PointSize = size * (150.0 / -mvPosition.z);
          gl_Position = projectionMatrix * mvPosition;
        }
      `,
      fragmentShader: `
        varying vec3 vColor;
        
        void main() {
          // Create soft circular points for dust
          float distanceFromCenter = length(gl_PointCoord - vec2(0.5));
          if (distanceFromCenter > 0.5) {
            discard;
          }
          
          // Soft dust with radial falloff
          float strength = 1.0 - (distanceFromCenter * 2.0);
          strength = pow(strength, 2.0);
          
          gl_FragColor = vec4(vColor, strength * 0.6); // Semi-transparent dust
        }
      `,
      transparent: true,
      blending: THREE.AdditiveBlending,
      depthWrite: false
    });
  }, []);
  
  // Animation for stars and dust
  useFrame(({ clock }) => {
    if (particlesRef.current) {
      const material = particlesRef.current.material as THREE.ShaderMaterial;
      material.uniforms.time.value = clock.getElapsedTime();
    }
    
    if (spiralDustRef.current && rotation) {
      // Slowly rotate the galaxy dust
      spiralDustRef.current.rotation.y += 0.0001;
      
      const material = spiralDustRef.current.material as THREE.ShaderMaterial;
      material.uniforms.time.value = clock.getElapsedTime();
    }
  });
  
  return (
    <group>
      {/* Background stars */}
      <points ref={particlesRef} geometry={starsGeometry}>
        <primitive object={starsMaterial} attach="material" />
      </points>
      
      {/* Spiral galaxy dust */}
      <points ref={spiralDustRef} geometry={spiralDustGeometry}>
        <primitive object={dustMaterial} attach="material" />
      </points>
    </group>
  );
};