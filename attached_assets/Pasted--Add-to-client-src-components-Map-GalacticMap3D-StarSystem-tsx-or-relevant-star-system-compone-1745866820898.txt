// Add to client/src/components/Map/GalacticMap3D/StarSystem.tsx
// or relevant star system component

import { useRef, useEffect } from 'react';
import { useFrame } from '@react-three/fiber';
import * as THREE from 'three';
import { useSpring, animated } from '@react-spring/three';

interface StarSystemProps {
  system: {
    id: string;
    name: string;
    type: string;
    x: number;
    y: number;
    z: number;
  };
  selected: boolean;
  onSelect: (id: string) => void;
}

// Star colors based on type
const starColors = {
  'main-sequence': 0xffffff,
  'red-giant': 0xff4500,
  'white-dwarf': 0xf0f8ff,
  'neutron-star': 0x00ffff,
  'black-hole': 0x000000,
  'binary': 0xffff00,
  'default': 0xffffff
};

export const PulsatingStarSystem: React.FC<StarSystemProps> = ({ system, selected, onSelect }) => {
  const starRef = useRef<THREE.Mesh>(null);
  const glowRef = useRef<THREE.Mesh>(null);
  
  // Get color based on star type
  const baseColor = starColors[system.type as keyof typeof starColors] || starColors.default;
  const starColor = new THREE.Color(baseColor);
  
  // Animation for selection
  const { scale } = useSpring({
    scale: selected ? 1.5 : 1,
    config: { tension: 300, friction: 10 }
  });
  
  // Pulsation effect
  useFrame((state) => {
    if (starRef.current && glowRef.current) {
      // Unique pulsation based on system id (using hash function)
      const hash = system.id.split('').reduce((a, b) => {
        a = ((a << 5) - a) + b.charCodeAt(0);
        return a & a;
      }, 0);
      
      // Use hash to create unique pulsation frequency and phase
      const frequency = 0.5 + (Math.abs(hash) % 10) / 20; // Between 0.5 and 1.0
      const phase = (Math.abs(hash) % 100) / 100; // Between 0 and 1
      
      // Calculate pulsation factor
      const pulse = Math.sin(state.clock.elapsedTime * frequency + phase) * 0.1 + 0.9; // 0.8 to 1.0
      
      // Apply to star
      starRef.current.scale.setScalar(pulse);
      
      // Apply to glow with larger effect
      glowRef.current.scale.setScalar(pulse * 1.3);
      
      // Glow opacity variation for more realism
      const material = glowRef.current.material as THREE.MeshBasicMaterial;
      material.opacity = pulse * 0.7;
    }
  });
  
  return (
    <group position={[system.x, system.y, system.z]} onClick={() => onSelect(system.id)}>
      {/* Main star */}
      <animated.mesh ref={starRef} scale={scale}>
        <sphereGeometry args={[0.3, 16, 16]} />
        <meshBasicMaterial color={starColor} />
      </animated.mesh>
      
      {/* Glow effect */}
      <mesh ref={glowRef}>
        <sphereGeometry args={[0.5, 16, 16]} />
        <meshBasicMaterial 
          color={starColor} 
          transparent={true} 
          opacity={0.5}
          blending={THREE.AdditiveBlending}
        />
      </mesh>
      
      {/* Selection indicator */}
      {selected && (
        <mesh>
          <ringGeometry args={[0.6, 0.7, 32]} />
          <meshBasicMaterial color={0xffffff} transparent={true} opacity={0.8} side={THREE.DoubleSide} />
        </mesh>
      )}
    </group>
  );
};